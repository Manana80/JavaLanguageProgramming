package day33_Statics;

public class TestIphone {
    public static void main(String[] args) {
      // static variables are shared by all the objects but they are independent of them, i.e. we don't need to create
        // an object to call them jsut call the class name and all the statics will appear.

        // if we want to call instance methods we need to create an object to call them

        System.out.println(iPhone.brand);
        System.out.println(iPhone.hasBattery);
















    }
}
